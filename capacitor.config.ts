import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.foroushino.app',
  appName: 'فروشینو',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
}

export default config
