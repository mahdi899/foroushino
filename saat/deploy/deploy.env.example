# Saat production server — local only, do not commit real hosts/passwords
# Copy to deploy.env (gitignored) and fill in.
DEPLOY_HOST=YOUR_SERVER_IP_OR_HOSTNAME
DEPLOY_PORT=22
DEPLOY_USER=root
DEPLOY_PASSWORD=
