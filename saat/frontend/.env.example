# Data layer switch (see src/services/index.ts). Defaults to "mock" when unset.
VITE_API_MODE=http
# Optional — in dev, API defaults to same-origin /api/v1 (Vite proxies to Laravel).
# For production build, set the full API URL:
# VITE_API_BASE_URL=https://api.example.com/api/v1
