import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.saat.app',
  appName: 'سات',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
}

export default config
