# Production build env for https://sat.center
# Copy to .env.production before `npm run build` on the server (or set in CI).

VITE_API_MODE=http
# Same-origin API via Nginx — do not set absolute API URL:
# VITE_API_BASE_URL=

VITE_UPDATE_TYPE=optional
VITE_TELEGRAM_BOT_USERNAME=RostamiAppBot
