# Let's Encrypt HTTP-01 (webroot) — required behind Cloudflare CDN.
# Include inside each :80 server block before any redirect to HTTPS.
location ^~ /.well-known/acme-challenge/ {
    root /var/www/certbot;
    default_type "text/plain";
    try_files $uri =404;
}
