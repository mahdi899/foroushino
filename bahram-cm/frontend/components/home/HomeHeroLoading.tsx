import { SiteRouteLoading } from '@/components/layout/SiteRouteLoading';

/** @deprecated Use SiteRouteLoading — kept for backwards compatibility. */
export function HomeHeroLoading() {
  return <SiteRouteLoading />;
}
