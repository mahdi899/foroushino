import { SiteRouteLoading } from '@/components/layout/SiteRouteLoading';

/** Instant feedback on soft navigations while the RSC payload arrives. */
export default function Loading() {
  return <SiteRouteLoading />;
}
