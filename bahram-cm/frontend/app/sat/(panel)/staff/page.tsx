export default function SatStaffPage() {
  return (
    <div>
      <h2 className="text-xl font-semibold">پرسنل</h2>
      <p className="mt-2 text-sm text-bone/70">مدیریت می‌تواند کارشناس و لیدر اضافه کند — فقط از API یا این صفحه.</p>
    </div>
  );
}
