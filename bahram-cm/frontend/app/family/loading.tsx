import { FamilyShellLoading } from '@/components/family/FamilyShellLoading';

/** Family route loading — skeleton only, never the full-page site loader. */
export default function FamilyLoading() {
  return <FamilyShellLoading />;
}
